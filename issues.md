Feature:

- Dynamic adaptibility, If I resize the pane or window the text of tasks stay hidden
- Ability to search task to see if it was already added or not
- undo a done task, we will have to think of the dynamics of how it will work in the md files
  - Best is that if I add a task 5 days ago, added few notes etc then marked it done 3 days ago
  - now today I want to undone it, we do this where we in the old entry add a note and something special(this is so code can recognize them) and then create the same task and notes on todays date
  - now that special something can be used to not show the same task twice
- Ability to delete note
- Add description to tags, I have tag like ahf, dcf, apv and sometime I forgot what
  - or if this is weird I think simpler option is to do a renaming of tags that would be easier and simpler
- In projects if I am on a project and click add, the tag should be automatically selected for that project
  - If on the side we have bb, and I do `a` it shouldn't ask me for tag, bb will automatically be the tag

- Ability to link to tasks and access/open them easily
  - if in bb-1 I say `ah continuing bb-5 with a modificattion` somehow I should be able to either see bb-5 or jump to it

- add ability to mark things priority wise, by default everything will be low
  - should be easily toggablable

Bug:

- If I stay in Project side and click n I still get option to add note and that is added to the first task that is not good
- When notes are added they are double tabbed not single
